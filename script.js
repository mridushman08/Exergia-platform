function joinClass(classId) {
    // Logic for joining the class could be expanded here (e.g., navigating to the class page)
    if (classId === 'class1') {
        window.location.href = 'class.html'; // Navigate to the class page (this would be dynamic for real apps)
    }
    if (classId === 'class2') {
        window.location.href = 'class.html'; // Navigate to the class page (this would be dynamic for real apps)
    }
    if (classId === 'class3') {
        window.location.href = 'class.html'; // Navigate to the class page (this would be dynamic for real apps)
    }
}